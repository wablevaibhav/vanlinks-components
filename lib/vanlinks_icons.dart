import 'package:flutter/material.dart';

class VanlinksIcons {
  VanlinksIcons._();

  static const Icon menu = Icon(Icons.menu);
  static const Icon close = Icon(Icons.close);
  static const Icon search = Icon(Icons.search);
  static const Icon notification = Icon(Icons.notifications);
  static const Icon arrowRight = Icon(Icons.arrow_forward_ios);
  static const Icon arrowLeft = Icon(Icons.arrow_back_ios);
}
