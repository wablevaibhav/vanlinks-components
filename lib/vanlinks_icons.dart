import 'package:flutter/material.dart';

class VanlinksIcons {
  VanlinksIcons._();

  // **Navigation Icons**
  static const Icon menu = Icon(Icons.menu);
  static const Icon close = Icon(Icons.close);
  static const Icon home = Icon(Icons.home);
  static const Icon arrowRight = Icon(Icons.arrow_forward_ios);
  static const Icon arrowLeft = Icon(Icons.arrow_back_ios);
  static const Icon arrowUp = Icon(Icons.keyboard_arrow_up);
  static const Icon arrowDown = Icon(Icons.keyboard_arrow_down);

  // **Action Icons**
  static const Icon add = Icon(Icons.add);
  static const Icon edit = Icon(Icons.edit);
  static const Icon delete = Icon(Icons.delete);
  static const Icon save = Icon(Icons.save);
  static const Icon share = Icon(Icons.share);
  static const Icon download = Icon(Icons.download);
  static const Icon upload = Icon(Icons.upload);
  static const Icon refresh = Icon(Icons.refresh);
  static const Icon send = Icon(Icons.send);

  // **Media Control Icons**
  static const Icon play = Icon(Icons.play_arrow);
  static const Icon pause = Icon(Icons.pause);
  static const Icon stop = Icon(Icons.stop);
  static const Icon volumeUp = Icon(Icons.volume_up);
  static const Icon volumeOff = Icon(Icons.volume_off);
  static const Icon microphone = Icon(Icons.mic);

  // **Form Control Icons**
  static const Icon check = Icon(Icons.check);
  static const Icon closeCircle = Icon(Icons.cancel);
  static const Icon warning = Icon(Icons.warning);
  static const Icon error = Icon(Icons.error);
  static const Icon info = Icon(Icons.info);
  static const Icon help = Icon(Icons.help);
  static const Icon calendar = Icon(Icons.calendar_today);
  static const Icon attachment = Icon(Icons.attachment);

  // **User & Profile Icons**
  static const Icon person = Icon(Icons.person);
  static const Icon accountCircle = Icon(Icons.account_circle);
  static const Icon logout = Icon(Icons.logout);
  static const Icon login = Icon(Icons.login);
  static const Icon settings = Icon(Icons.settings);
  static const Icon profile = Icon(Icons.person_outline);
  static const Icon userGroup = Icon(Icons.group);

  // **Social Icons**
  static const Icon like = Icon(Icons.thumb_up);
  static const Icon favorite = Icon(Icons.favorite);
  static const Icon comment = Icon(Icons.comment);
  static const Icon bookmark = Icon(Icons.bookmark);

  // **Commerce Icons**
  static const Icon shop = Icon(Icons.store);
  static const Icon payment = Icon(Icons.payment);
  static const Icon wallet = Icon(Icons.account_balance_wallet);
  static const Icon money = Icon(Icons.money);
  static const Icon receipt = Icon(Icons.receipt);

  // **Feedback & Status Icons**
  static const Icon success = Icon(Icons.check_circle);
  static const Icon errorCircle = Icon(Icons.error);
  static const Icon infoCircle = Icon(Icons.info);
  static const Icon warningCircle = Icon(Icons.warning_amber_rounded);
  static const Icon loading = Icon(Icons.hourglass_empty);
  static const Icon spinner = Icon(Icons.sync);
  static const Icon progress = Icon(Icons.timelapse);

  // **Utility & General Icons**
  static const Icon search = Icon(Icons.search);
  static const Icon filter = Icon(Icons.filter_list);
  static const Icon location = Icon(Icons.location_on);
  static const Icon qrCode = Icon(Icons.qr_code);
  static const Icon barcode = Icon(Icons.qr_code_scanner);
  static const Icon lock = Icon(Icons.lock);
  static const Icon unlock = Icon(Icons.lock_open);
}
